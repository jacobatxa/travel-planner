# Travel Planner v2 — 功能补全任务文档

## 架构决策

### 代码编写方式
- **主代码改动用 `delegate_task` + `patch`**（已验证可行，之前8次patch全部成功）
- **避免重写整个文件**，用增量patch逐步修改
- 如果需要大规模新功能（如AI对话规则引擎），拆成独立模块再 `import` 或内联

### 代码风格约束（Rules）
1. 全部用 `const` / `let`，不用 `var`
2. CSS类名用 `-` 连接（kebab-case），不用驼峰
3. 中文注释，关键逻辑必须有注释
4. 不使用任何第三方库（除了已加载的 Leaflet + Google Fonts）
5. 数据对象用大写开头（如 `Locs`、`S`、`I`），函数用小写开头
6. AI对话相关函数统一 `chat*` 前缀

### 迭代策略
- 每完成1个功能 → 本地验证（浏览器打开检查）
- 全部完成后 → 一次 git commit + push 部署

---

## 任务列表（优先级排序）

### 🔴 P0 — 核心缺陷

#### 1. AI对话功能 —— 前端规则引擎
**描述**：`sendChat()` 现在是模拟回复，需要改成真正的意图解析器。
**技术方案**：
- 纯前端，不调后端API
- 写一个 `chatParseIntent(text, Locs, S, I)` 函数
- 支持指令：
  - `把[景点]改到第N天` → 重新排序行程
  - `加一天去[地点]` → 插入新行程项
  - `删掉[地点]` → 删除行程项
  - `帮我看看每天要开多久` → 计算每日总驾车时间
  - `[景点]有什么玩的` → 查看景点详情
- 操作 `Locs`、`S`、`I` 数据对象后，调用 `chatReRender()` 重新渲染
- **复杂度高** → 需单独delegate

#### 2. 总里程数据同步（P1 #6）
**描述**：panel-header显示 `约1,384km`，OSRM真实数据是 `约1,625km`
**修复**：搜索文件中的 `1,384` 替换为 `1,625`，同时更新 `约¥7,500` 为精确估算

---

### 🟠 P1 — 重要体验优化

#### 3. 距离标注与路段高亮联动
**描述**：点击行程项后，高亮路段的标注应该变亮/变大，非高亮路段标注变半透明
**方案**：给每个标注marker存引用，在 `go()` 函数中更新标注样式

#### 4. 每日开车总时长汇总
**描述**：每个Day-header下加一行 `今日驾车：~XhYmin / Zkm`
**方案**：根据路段数据和时间数据计算，动态渲染在day-header中

#### 5. "复位"按钮（自由地图探索）
**描述**：加一个悬浮按钮，点击后地图回到环线全景视图
**方案**：右下角加一个 `🗺️` 悬浮按钮，调用 `map.fitBounds(full.getBounds().pad(0.08))`

---

### 🟡 P2 — 锦上添花

#### 6. 景点弹窗信息加强
**描述**：弹窗中加推荐游玩时间、门票价格、避坑提醒
**方案**：扩充 `Locs` 数据，弹窗模板升级

#### 7. 加载动画
**描述**：页面加载时加个loading spinner，地图渲染完成后再隐藏
**方案**：CSS动画 + `map.on('load', ...)`

#### 8. 一键导航按钮
**描述**：景点弹窗中加 `🚗 高德导航` / `🚗 百度导航` 链接
**方案**：用高德/百度地图URL Scheme

---

### 🔵 P3 — 长期方向（本次不做）

- 多行程管理
- 通用化（非恩施硬编码）
- 分享行程
- 打印/导出PDF
- 实时天气/路况

---

## 开发顺序

```
第1步：修复总里程数据（简单patch，2分钟）
第2步：复位按钮（简单，5分钟）
第3步：加载动画（简单，5分钟）
第4步：每日开车总时长（中等，10分钟）
第5步：标注联动（中等，15分钟）
第6步：弹窗信息加强（中等，15分钟）
第7步：一键导航按钮（中等，10分钟）
第8步：AI对话规则引擎（复杂，30-60分钟）
```

总计预估：~2小时
